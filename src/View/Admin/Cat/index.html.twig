{% block stylesheet %}
    <link rel="stylesheet" href="/assets/css/banner.css">
    <link rel="stylesheet" href="/assets/css/style-admin.css">
    <link rel="stylesheet" href="/assets/css/admin-cat.css">
{% endblock %}

{% block content %}
    <div class="banner"></div>
        <div class="title">
            <h1>Admin / Chats</h1>
        </div>
            <div class="container-sm">
                <div class="card card-entity d-flex flex-row mb-5">
                    <a href="/admin/cat/ajouter" class="btn btn-entity btn-circle d-flex justify-content-center align-items-center align-self-center ms-4 btn-lg">+</a> 
                    <div class="card-body card-entity-title d-flex justify-content-center">Titre</div>
                    <div class="card-body card-entity-date d-flex justify-content-center">Date</div>
                </div>
                    {% for cat in cats %}
                        <div class="card card-cat d-flex flex-row mb-5 p-3 flex-column flex-sm-row">
                            <div class="card-title-cat d-flex flex-row align-items-center gap-3">
                                <img src="/assets/images/newspaper.svg" class="icon-newspaper" alt="icon-newspaper">
                                <div>{{ cat.title }}</div>
                            </div>
                                <div class="card-body d-flex flex-row justify-content-end align-items-center flex-column flex-sm-row">
                                    <div class="card-date me-3 pr-3">{{ cat.date }}</div>
                                    <a href = "/admin/cat/editer?id= {{ cat.id }}" class="btn btn-edit d-flex align-items-center me-3 btn-primary">Éditer</a>
                                </div>
                        </div>
                    {% endfor %}
            </div>
{% endblock %}