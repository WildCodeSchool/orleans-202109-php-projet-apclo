{% extends 'layoutAdmin.html.twig' %}

{% block stylesheet %}
	<link rel="stylesheet" href="/assets/css/banner.css">
	<link rel="stylesheet" href="/assets/css/style-admin.css">
{% endblock %}
{# 
{% block content %}
	<div class="container">
		<div class="row justify-content-between text-center">
			{% for cat in cats %}
				<div class="cat col-sm-2">
					<div class="member-image">
						<img class="card-img-top" src={% if cat.image is not empty %} "assets/uploads/{{ cat.image}}" {% else %} "assets/images/silhouette.png" {% endif %} alt="cat-image">
					</div>
					<div class="cat-name">
						<p>
							{{ cat.name }}
							{{ cat.birth_date }}
							{{ gender.id }}
							{{ color.id }}
							{{ breed.id }}
							{{ furr.id }}
							{{ cat.adoption_date }}
						</p>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}


{% block content %}
	<div class="banner"></div>
	<div class="title">
		<h1>Admin / Chats</h1>
	</div>
	<div class="container-sm">
		<div class="card card-entity d-flex flex-row mb-5">
			<a href="/admin/actualites/ajouter" class="btn btn-entity btn-circle d-flex justify-content-center align-items-center align-self-center ms-4 btn-lg fs-3">+</a>
			<div class="card-body card-entity-name d-flex justify-content-center fs-3">Nom</div>
			<div class="card-body card-entity-date_of_birth d-flex justify-content-center fs-3">Date de naissance</div>
		</div>
		{% for actuality in actualities %}
			<div class="card card-actuality d-flex flex-row mb-5 p-3 flex-column flex-sm-row">
				<div class="card-title-actuality d-flex flex-row align-items-center gap-3">
					<img src="/assets/images/newspaper.svg" class="icon-newspaper" alt="icon-newspaper">
					<div class="card-actuality fs-5">{{ actuality.title }}</div>
				</div>
				<div class="card-body d-flex flex-row justify-content-end align-items-center flex-column flex-sm-row">
					<div class="card-date me-3 pr-3 fs-5">{{ actuality.date }}</div>
					<a href="/admin/actualites/editer?id= {{ actuality.id }}" class="btn btn-edit d-flex align-items-center me-3 btn-primary">Éditer</a>
					<form method="POST" action="/admin/actualités/supprimer">
						<input type="hidden" name="id" value="{{ actuality.id }}">
						<button class="btn btn-delete btn-primary mt-4">Supprimer</button>
					</form>
				</div>
			</div>
		{% endfor %}
	</div>
{% endblock %} #}


{% block content %}
	<div class="banner"></div>
	<div class="title">
		<h1>Admin / Chats</h1>
	</div>

	<table class="table">
		<thead>
			<tr>
				<th scope="col">
					<a href="/admin/chats/ajouter" class="btn btn-entity btn-circle d-flex justify-content-center align-items-center align-self-center ms-4 btn-lg fs-3">+</a>
				</th>
				<th scope="col">Nom</th>
				<th scope="col">Genre</th>
				<th scope="col">Date de naissance</th>
			</tr>
			{% for cat in cats %}
				<tr>
				<td class="">{{ cat.image }}</td>
				<td class="">{{ cat.name }}</td>
				<td class="">{{ cat.gender }}</td>
				<td class="">{{ cat.birth_date }}</td>
				</tr>
			{% endfor %}
		</thead>
	</table>


{% endblock %}
